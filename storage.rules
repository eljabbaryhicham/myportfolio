
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Only allow authenticated users to access the 'uploads' folder
    match /uploads/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
