
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow admin users to read and write to the uploads folder
    match /uploads/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
